# Fixtures

Shared sample data used across all platform test suites:

- Sample M3U / M3U8 playlists
- Sample XMLTV (EPG) files
- Xtream Codes API mock responses (JSON)

These files are kept as a single source of truth; each app references them from its own test layer.
